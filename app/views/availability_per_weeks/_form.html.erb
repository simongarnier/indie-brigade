<%= form_for(@availability_per_week) do |f| %>
  <% if @availability_per_week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability_per_week.errors.count, "error") %> prohibited this availability_per_week from being saved:</h2>

      <ul>
      <% @availability_per_week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
