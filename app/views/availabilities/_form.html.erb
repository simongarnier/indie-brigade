<%= form_for(@availability) do |f| %>
  <% if @availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability.errors.count, "error") %> prohibited this availability from being saved:</h2>

      <ul>
      <% @availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :availability_per_week_id %><br>
    <%= f.text_field :availability_per_week_id %>
  </div>
  <div class="field">
    <%= f.label :availability_duration_id %><br>
    <%= f.text_field :availability_duration_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
