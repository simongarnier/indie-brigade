<%= form_for(@availability_duration) do |f| %>
  <% if @availability_duration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability_duration.errors.count, "error") %> prohibited this availability_duration from being saved:</h2>

      <ul>
      <% @availability_duration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
